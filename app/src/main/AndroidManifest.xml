<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.headlit.birthdaywishingapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/birthday"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirthdayWishingApplication">
        <activity android:name="com.headlit.birthdaywishingapplication.BdayWishingActivity"></activity>
        <activity
            android:name="com.headlit.birthdaywishingapplication.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>